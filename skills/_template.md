---
name: template
description: Template skill - copy this to create new skills
version: "1.0"
# triggers:
#   - "example phrase"
# Skills WITHOUT triggers are prompt-only: always loaded into the prompt,
# and the LLM decides when to apply them (e.g., summarize, translate).
#
# Skills WITH triggers are tool skills: the daemon matches trigger phrases
# against the user's message, runs a daemon-side action (code in tools/),
# and injects the results into the prompt (e.g., web_search).
enabled: false
author: system
---

# Template Skill

This is a template. Copy this file and rename it to create a new skill.

## Instructions

Replace this section with instructions for the agent when this skill is activated.
